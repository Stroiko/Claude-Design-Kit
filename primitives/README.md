# /primitives/

Industry-agnostic base primitives vendored from shadcn/ui (button, input, card, badge, dialog, tabs, …). Style comes from CSS variables, so the same primitive renders correctly under any industry's palette. Import as `@/primitives/<name>`.
